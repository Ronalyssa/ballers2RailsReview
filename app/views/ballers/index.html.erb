<h1> Here is the list of all the Ballers </h1>

<ul>
  <% @ballers.each do |baller| %>
    <li><%= link_to baller.name, baller_path(baller) %></li>
  <% end %>
</ul>

<%= link_to "Add New Baller", new_baller_path %>